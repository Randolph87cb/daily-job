# 项目仓库初始化与发布

- 时间：2026-05-09
- 目标：把 `D:\workspace\日常事务` 整理为可发布的 Git 仓库，补齐项目级 `AGENTS.md`、同步目录文档、清理临时文件、配置忽略规则，并推送到 `https://github.com/Randolph87cb/daily-job.git`。
- 初始现状：
  - 当前目录还不是 Git 仓库。
  - 根目录没有项目自己的 `AGENTS.md`。
  - 根目录没有统一 `README.md`。
  - 已发现一次性临时产物主要位于 `.tmp/`、`.playwright-mcp/` 和若干输出目录下的嵌套 `.tmp/`。
- 已完成修改：
  - 新增根目录 `AGENTS.md`，写入项目目录结构、文档同步规则、临时文件规则和 Git 规则。
  - 新增根目录 `README.md`，补齐项目用途、目录说明和维护约定。
  - 新增根目录 `.gitignore`，忽略 `.env`、`.tmp`、`.playwright-mcp`、`node_modules`、`__pycache__`、日志等本机或可再生内容。
- 已清理临时文件：
  - 删除根目录 `.tmp/`
  - 删除根目录 `.playwright-mcp/`
  - 删除项目内 `__pycache__/`
  - 删除 AtCoder 输出目录中的嵌套 `.tmp/` 导出缓存
- Git 处理结果：
  - 已初始化 Git 仓库，主分支为 `main`
  - 已配置远端：
    - `https://github.com/Randolph87cb/daily-job.git`
  - 已创建中文提交：
    - `40b4c9e 初始化日常事务项目仓库`
  - 已推送到远端 `origin/main`
- 验证：
  - `git status --short --branch` 在提交后返回干净状态
  - `git status --short --ignored` 确认 `.env` 和 `node_modules/` 已被忽略
  - 清理后重新扫描，目标临时目录不再参与版本控制

# 项目仓库初始化与发布

- 时间：2026-05-09
- 目标：把 `D:\workspace\日常事务` 整理为可发布的 Git 仓库，补齐项目级 `AGENTS.md`、同步目录文档、清理临时文件、配置忽略规则，并推送到 `https://github.com/Randolph87cb/daily-job.git`。
- 初始现状：
  - 当前目录还不是 Git 仓库。
  - 根目录没有项目自己的 `AGENTS.md`。
  - 根目录没有统一 `README.md`。
  - 已发现一次性临时产物主要位于 `.tmp/`、`.playwright-mcp/` 和若干输出目录下的嵌套 `.tmp/`。
- 已完成修改：
  - 新增根目录 `AGENTS.md`，写入项目目录结构、文档同步规则、临时文件规则和 Git 规则。
  - 新增根目录 `README.md`，补齐项目用途、目录说明和维护约定。
  - 新增根目录 `.gitignore`，忽略 `.env`、`.tmp`、`.playwright-mcp`、`node_modules`、`__pycache__`、日志等本机或可再生内容。
- 待执行：
  - 清理现有临时文件与缓存目录。
  - 初始化 Git 仓库并连接远端。
  - 提交、推送并记录验证结果。
